# Global LLM configuration
[llm]
model = "qwen-plus"
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
api_key = "sk-YOUR_API_KEY_HERE"
max_tokens = 4096
temperature = 0.0


[database]
host = "127.0.0.1"
port = 3306
user = "root"
password = "your_password"
database = "exp_data"
charset = "utf8mb4"

[services]
evaluation_api_url = "http://127.0.0.1:13338/RECIPE/recipe_recommendation"
http_timeout_sec = 300


# Optional configuration for specific LLM models
[llm.vision]
model = "qwen-plus"
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
api_key = "sk-YOUR_API_KEY_HERE"
max_tokens = 4096
temperature = 0.0

# Optional Runflow configuration
[runflow]
use_data_analysis_agent = false

# RecipeQA LLM configuration
[recipeqa_llm]
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
dashscope_api_key = "sk-YOUR_API_KEY_HERE"
dashscope_model = "qwen-plus"
temperature = 0.4
timeout = 60

# DeepSeek configuration for edge reporting
[deepseek]
api_key = "sk-YOUR_DEEPSEEK_API_KEY_HERE"
base_url = "https://api.deepseek.com/v1/chat/completions"
model = "deepseek-reasoner"
temperature = 0.3
timeout = 120

# RecipeQA database configuration
[recipeqa.database]
host = "127.0.0.1"
port = 3306
user = "root"
password = "your_password"
database = "exp_data"
charset = "utf8mb4"

# Reasoning module configuration
[reasoning_database]
host = "127.0.0.1"
port = 3306
user = "root"
password = "your_password"
database = "exp_data"
charset = "utf8mb4"
table = "daily_experiment"

[reasoning_output_database]
host = "127.0.0.1"
port = 3306
user = "root"
password = "your_password"
database = "exp_data"
charset = "utf8mb4"
table = "MIRecipe"

[reasoning_llm]
base_url = "http://localhost:8000"
dashscope_api_key = "sk-YOUR_API_KEY_HERE"
dashscope_model = "qwen-plus"
temperature = 0.7
timeout = 120

[reasoning_generation]
total_runs = 15
max_workers = 10

# Generating module configuration
[generating_database]
host = "127.0.0.1"
port = 3306
user = "root"
password = "your_password"
database = "exp_data"
charset = "utf8mb4"

[generating_output_database]
host = "127.0.0.1"
port = 3306
user = "root"
password = "your_password"
database = "exp_data"
charset = "utf8mb4"

[generating_llm]
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
dashscope_api_key = "sk-YOUR_API_KEY_HERE"
dashscope_model = "qwen-plus"
deepseek_api_key = "sk-YOUR_DEEPSEEK_API_KEY_HERE"
deepseek_base_url = "https://api.deepseek.com/v1/chat/completions"
deepseek_model = "deepseek-reasoner"
temperature = 0.7
timeout = 120

[generating_generation]
total_runs = 15
max_workers = 10

# Learning module configuration
[learning_database]
host = "127.0.0.1"
port = 3306
user = "root"
password = "your_password"
database = "exp_data"
charset = "utf8mb4"

[learning_llm]
model = "qwen-plus"
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
api_key = "sk-YOUR_API_KEY_HERE"
max_tokens = 4096
temperature = 0.0

# Evaluation module LLM configuration
[evaluation_llm]
deepseek_api_key = "sk-YOUR_DEEPSEEK_API_KEY_HERE"
deepseek_base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
deepseek_model = "deepseek-r1"
qwen_api_key = "sk-YOUR_API_KEY_HERE"
qwen_base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
qwen_model = "qwen-max"
gpt5_api_key = "sk-YOUR_GPT5_API_KEY_HERE"
gpt5_base_url = "https://api.openai.com/v1/"
gpt5_model = "gpt-5"
default_llm_key = "qwen"

# Evaluation module predictor configuration
[evaluation_predictor]
base_model_dir = "data/predictor_inputs"

# Evaluation module configuration
[evaluation]
compound_mapping = "data/compound_mapping.json"
materials_dict = "data/materials_dict_2025_11_11.pickle"
rubric_path = "data/five_dimension_rubrics_new_zhao.json"

recipe_indicators = [
    "recipe_integrity",
    "formula_rationality",
    "parameter_rationality",
    "performance_rationality",
    "recipe_recommendation",
    "experimental_validation"
]
mechanism_indicators = [
    "domain_knowledge",
    "mechanism_integrity",
    "mechanism_interpretation",
    "mechanism_comprehensiveness",
    "mechanism_coherence"
]

recipe_custom = [
    "recipe_integrity",
    "formula_rationality",
    "parameter_rationality",
    "experimental_validation"
]
mechanism_custom = [
    "domain_knowledge",
    "mechanism_integrity",
    "mechanism_interpretation",
    "mechanism_comprehensiveness",
    "mechanism_coherence"
]

[evaluation.indicator_weight]
recipe_integrity = 0.05
formula_rationality = 0.05
parameter_rationality = 0.05
performance_rationality = 0
recipe_recommendation = 0
experimental_validation = 0.35
domain_knowledge = 0.1
mechanism_integrity = 0.1
mechanism_interpretation = 0.1
mechanism_comprehensiveness = 0.1
mechanism_coherence = 0.1

system_prompt = """
You are an expert reviewer of perovskite-solar-cell research papers and a strict but fair grader.

Your task:
- Read a JSON rubric and a JSON object containing one model-generated report.
- Assign integer scores and short comments for each rubric dimension.
- Follow the rubric exactly and be slightly conservative.
- Respond ONLY with a single valid JSON object, with no markdown and no extra text.
"""

# Local LLM configuration (for internal deployment)
[local_llm]
base_url = "http://localhost:8000"
model = "your-local-model-name"
temperature = 0.7
timeout = 120
